object Euler15  {
    def main(args:Array[String])  {
        // Since we only turn Right, and Down - we need a 40 character string
        // which is RRDRR or RDRRD, etc. With 20 Rs, and 20 Ds.
        //
        // In other terms - redefine the problem as having 20Rs and 20Ds and
        // finding the number of ways to place them. 40! is the number of permutations
        // if Rs and Ds are not identical, but they are, so the answer is
        //
        // 40! / (20! * 20!)
        
    }
}

// vim: set ts=4 sw=4 et:
